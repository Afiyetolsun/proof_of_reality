# Public RPC works for the demo; pin to Alchemy/Infura for stability.
BASE_SEPOLIA_RPC=https://sepolia.base.org

# Funded deployer wallet (Sepolia faucet ETH).
DEPLOYER_PK=

# Backend's hot wallet — granted MINTER_ROLE at deploy time. Address only, not the PK.
BACKEND_MINTER_ADDR=

# Basescan API key (free signup) — for `verify`.
BASESCAN_API_KEY=
